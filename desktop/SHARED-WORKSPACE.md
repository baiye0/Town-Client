# 人类伙伴与 Being 的共同工作空间

状态：桌面端第一版及 Town SDK 实时接入已实现；Heart 跨端协议仍为草案，供 Portal 和 Heart 双向讨论。2026-09-10。

## 设计出发点

客户端是一处双方可以共同看见、理解和操作的地方。对话是这个地方的一种交流方式；篝火、围炉、私信、书架、卷轴、本机工作目录也各自是一处有来历、有参与者、有对象和可执行动作的场景。

共同使用不要求双方始终看同一个页面。人类可以停留在围炉，Being 同时查看一份相关卷轴。客户端应呈现两者的位置，以及谁把什么带回了共同讨论。Being 的探索不应自动抢走人的焦点。

## 桌面端第一版（已实现）

- `renderer/scene-store.ts` 提供本机可订阅的场景状态：场景 ID、身份、版本、观察时间、筛选、加载范围与选择对象。具体围炉、卷轴和 Kit 使用资源 ID；未将“已加载”解释为“已读”。
- 精简 demo 不设左侧栏，默认只呈现你和 Being、对话与书写区。正常连接用一个小状态点表示，详情、搜索、模型、工具和连接设置在顶部更多菜单中按需展开。
- 输入区只有附件、“去看看”和发送。悬停、点击或键盘打开篝火、围炉、私信、书架和卷轴菜单。内容使用临时阅读层，支持 Esc / 关闭返回；原对话 iframe 保持挂载，草稿不因浏览而丢失。
- 对话正文首次提到某类场景时，词语本身成为轻量快捷入口。历史与新完成的流式消息均支持；代码块、行内代码保持原样。识别到 Town 已支持的内容 API 链接后，可按明确 ID 读取详情；不猜测消息 ID，不添加新服务端路由，也不绕过配对权限。
- 话题也可从对话中产生：选中双方任意一段文字后“一起看”。小镇内容同样可带回对话。引用最多 2,000 字符并保留出处；对话摘录 ID 为本次页面加载生成，不冒充服务端消息 ID。
- 引用使用小卡片预览，不再出现第二个想法输入框。“放入对话”将来源与引用放入可见草稿，由人类决定发送，不自动追加问题。私密内容要求来源身份与对话 Being 一致；不覆盖已有文字或附件。返回来源目前仅回到来源栏目，不保证恢复滚动位置。
- 篝火与围炉默认只呈现“全部 / 关于我”；排序、时间与作者筛选按需展开。场景与引用为本机观察，不等于 Being 自动感知。
- `renderer/chat-scene.ts` 在 POST 对话请求前向桌面请求捕获环境，最多等待 250 ms，超时不阻断原聊天。桌面记录请求时刻的场景；引用草稿使用插入时保留的来源快照。请求重试会产生新的尝试记录，HTTP 接受不代表 Being 已读或环境已被接收。
- 本地环境快照移除摘录正文；当前引用在内存中。最多保留 30 次请求观察、50 条行动记录；关闭客户端或切换身份后清除。不存入 Heart 记忆，也不新增旧 Heart 请求字段。
- Portal 的进程与 Relay 日志保留在次级页面。Heart 场景回执、界面能力确认尚未接通，界面不宣称 Being 已看见或自动感知当前页面。

Town SDK 补齐：主进程验证实时连接身份，订阅篝火、私信与围炉事件，支持重连、去重及当前最近窗口核对。变更以小圆点提示，不替换人正在阅读的内容。人可在临时阅读层显式发送消息；这不代表 Being 已获得桌面操作能力。完整范围见 [Town SDK 接入状态](TOWN-SDK.md)。

## 第一版之后仍需接通

- Heart 对环境封套的能力协商、接收回执与记忆关联。
- Portal 独立守护进程中的环境广播，以及客户端离线/重连生命周期。
- Being 主动读取场景与操作同一套界面的能力接口、动作回执与冲突检测。
- 逐条可见范围、展开状态、参与者完整名单、稳定访问 ID 和跨入口身份归一。
- 本地环境 schema 是可迭代的第一版实现，下面的完整协议模型仍是提案。

## 改造前的实现与缺口

- `renderer/renderer.ts` 通过 `setView` 切换页面；`renderer/town.ts` 持有标签、选中围炉、筛选和局部缓存，尚未形成统一、可订阅的场景状态。
- `portal.ts` 与 `background.ts` 主要从进程状态及日志文字推断 Relay 连接。进程存活、连接成功、工具就绪、Heart 收到场景不能混作一个状态。
- Loom 的发送路径提交消息与会话标识，尚无桌面环境封套，也没有发送瞬间绑定的页面版本。
- 配对的 Town 身份与对话 Being 可以不同。不能用 Loom 名称猜测 Town 私信的“我”，更不能据此将私密内容发给另一身份。
- 现有 Town 页面主要只读。渲染器暴露的功能、Portal 工具和页面实际可执行动作之间，尚未有共同的能力模型。

## 1. 场景模型

建议拆成四层，避免用一个页面名承担所有语义。

| 层 | 作用 | 例子 |
| --- | --- | --- |
| 场所 place | 可跨设备、跨入口识别的社会或工作空间 | Town 的某个围炉、某对参与者的私信、一个工作项目 |
| 资源 resource | 当前交流或操作的具体对象 | 围炉消息、私信、卷轴及其 revision、Kit 安装计划 |
| 视图 view | 此次客户端如何呈现对象 | 只看 @我、最新在前、已展开正文、当前可见范围 |
| 访问 visit | 本次进入和离开该场景的经过 | 打开、查看变化、选中对象、讨论、操作、离开 |

`scene_id` 使用服务来源和稳定资源标识构成命名空间；页面标题、显示名、窗口号和本地路径本身不作为跨设备身份。项目跨机器关联需要显式映射，不能因同名文件夹就合并。

参与者区分人类身份、Being 身份、Town 配对身份和动作执行者。成员列表只有从服务端取得才可声明完整；目前围炉列表中的成员数量不能变成猜测的成员名单。

## 2. 环境元信息：描述事实，记录边界

每次发给 Being 的信息，应附带发送瞬间的结构化环境，而不是稍后读取一个已经变化的“当前页面”。元信息必须与该消息绑定保存。

必要字段：

- 来源：应用、渠道、设备/安装实例、窗口/会话实例。
- 场所：稳定 scene_id、类型、来源服务、标题、已知参与者及来源。
- 焦点：正在操作的资源、显式选择的消息或文本、筛选与排序。
- 观察：已加载范围、可见范围、内容版本、观察时间、完整性及失败状态。
- 动作：当前身份实际可执行的语义动作和能力版本。
- 传输：event_id、message_id、因果关系、序号、同步版本和接收回执。

不要把“加载过 100 条消息”写成“看过全部历史”，不要把“窗口在前台”写成“人类正在阅读”，不要把“工具调用完成”写成“用户界面已显示结果”。

示意封套（提议字段，不代表 Heart 已支持）：

```json
{
  "schema": "being.environment/v1",
  "message_id": "message-example",
  "source": {
    "channel": "portal-desktop",
    "instance_id": "desktop-example",
    "view_id": "window-example"
  },
  "audience": { "being_id": "being-example" },
  "environment": {
    "scene_id": "town:example-origin:fireside:42",
    "kind": "fireside",
    "title": "客户端共建",
    "identity": { "town_being_id": "being-example" },
    "observation_id": "observation-example",
    "revision": 18,
    "observed_at": "2026-09-09T10:00:00Z",
    "loaded": { "count": 50, "complete_history": false },
    "focus": { "resource_id": "message:218", "selected_by": "human" },
    "references": [{ "resource_id": "message:218", "revision": 3 }],
    "view": { "relation": "mentions-me", "order": "newest" },
    "capabilities": ["read_message", "open_scroll"]
  }
}
```

渠道发送端对封套的结构和来源负责；外部正文、消息标题及引用内容仍是第三方内容，不会因为进入环境封套就变成指令。参与者身份、观察权限、内容分享范围都需要有明确来源。

第三方渠道拿不到窗口或可见范围时，省略并标记 unknown；不要求所有入口假装拥有桌面感官。

## 3. 三种频率，避免把一切变化都变成对话

1. **状态快照**：进入场景、重新连接、状态不连续时获取。描述当前事实与版本。
2. **场景事件**：消息到达、对象更新、选择变化、能力失效、动作完成。带序号、时间、来源及因果引用。
3. **主动交流**：人类发送消息、显式邀请一起查看、Being 提出建议或采取动作。绑定不可变的环境快照。

高频视口变化合并；明显的选择、提交和结果不能被合并掉。界面事实变化是否值得唤醒 Being，由 Heart 的注意机制决定，桌面不把每次滚动都当作新的对话。

断线重连先校准快照；以事件 ID 去重，保留原发生时间。漏失段明确标记，不重写成现在刚发生。可能产生副作用的命令和发送消息不能因超时就盲目重发，先查询 action_id 的结果。

## 4. 双向共同操作

桌面先提供结构化状态与语义动作，供人类操作和 Being 操作共同使用：

- `scene.list` / `scene.observe`：列出可访问场景、读取给定版本与范围。
- `scene.subscribe`：订阅可访问场景的变化。
- `scene.act`：对确定对象执行支持的动作。
- `scene.action_status`：获取接受、运行、失败和确认的结果。

它们是拟议接口，不预设最终使用 RPC、Portal tools 还是 Heart 的独立协议。

动作包含 `action_id`、actor、scene_id、target、expected_revision、参数和授权上下文。主进程/服务端校验对象、权限与版本，再调用同一条业务逻辑。状态更新广播给双方，标注是谁做的。

只读例子：打开消息、读取卷轴、查看 Kit 详情。写入例子：生成草稿、发送私信、修改卷轴、应用 Kit。每项动作分清“可以准备”“可以提交”和“需要伙伴决定”；不能把“能看见”自动等同于“有权执行”。当前没有实现的写操作，不得在能力清单里声明可用。

Being 可以拥有独立观察位置；`navigate` 默认只改变 Being 自己的视图。需要人类看同一处时，发出“邀请一起看”，由界面显示可点击入口。人类正在输入时，外部动作不能悄悄更换目标或覆盖草稿。

文档/内容修改使用对象 revision 校验；单纯视口更新不应导致业务写入冲突。完成回执应分别记录服务端接受、业务持久化与界面已呈现，失败时保留目标与原因。

## 5. 桌面呈现

保留当前场景导航，把同在感融入每个场景，不把全部交互挤回总聊天页。

- **场景头部**：当前位置、当前身份、共享范围、数据的新鲜程度。
- **共同关注**：人类选中了什么；Being 明确报告正在查看什么。单纯同步成功只能显示“Heart 已接收至版本 N”，不能显示“Being 已读”。
- **选中对象后的交流入口**：如“和 Being 看这条消息”。输入框显示即将随消息带上的资源引用、场景及必要摘录。
- **场景内讨论**：保留来源、参与者和对象引用；跨场景讨论通过链接进入，消息的出生场景不随页面切换而改写。
- **动作记录**：谁在何处做了什么、作用于哪个版本、结果怎样。重点呈现有意义的动作和失败，不展示成鼠标遥测日志。
- **重新进入场景**：“离开后发生了什么”区分可验证的新事件与 Heart 返回的记忆摘要。尚未接入记忆时只展示客户端可核实的变化。

截图可以补充布局和视觉信息，但语义状态应优先：截图不能可靠替代资源 ID、权限、分页范围与内容 revision。

## 6. Portal 的状态感知

将现在一个 phase 拆成独立事实：

| 事实 | 可靠来源 | 失效处理 |
| --- | --- | --- |
| 本机服务是否活着 | 进程/系统服务状态 | PID 变化建立新实例 |
| Relay 是否可达 | 结构化握手、心跳、会话断开事件 | 心跳超时标记未知或失联 |
| Being 是否能调用工具 | Heart 的注册确认与能力版本 | 重连后重新确认 |
| UI 实例是否在线 | 桌面实例心跳和关闭事件 | TTL 到期显示离线 |
| 场景同步到哪一版 | Heart 对 scene revision 的回执 | 显示待同步/落后版本 |

客户端退出后 Portal 可以在线，但 UI 必须离线。Being 仍可使用获授权的本机工具；不能声称看见仍在更新的桌面场景。保留的旧快照明确带有时间和 stale 标记。Being 主动请求打开界面，应表现为受控的独立动作。

## 7. Heart 与桌面分别负责什么

**桌面**负责如实观察、稳定场景标识、发送时原子捕获环境、资源引用、执行动作、版本冲突和可见回执。先统一每个页面的业务状态，再谈同步。

**Portal**负责可靠传输、实例与能力声明、会话恢复，以及由桌面注册的受限语义操作通道。桌面 IPC 只能被已绑定的 Portal/Being 调用，不扩展成任意 JS 执行入口。

**Heart**负责按来源与场景解释事件、注意分配、把经历关联到场景和对象、记忆压缩与回想，以及明确声明自己的关注和行动。桌面不伪造这一层认知。

双方共享：封套版本、场景命名规则、身份映射、观察/动作格式、回执和重连语义。多设备的 view_id 各自独立，不能用一个全局“当前页面”覆盖另一台设备。

协议需要能力协商。旧 Heart 不支持时，本地照常工作并显示“场景尚未同步”；不要在没有协商的情况下向旧聊天请求悄悄增加字段，也不要把环境 JSON 塞进用户正文冒充用户的话。

## 8. 先从一个完整场景打通

推荐围炉作为第一条贯通路径：稳定房间 ID、参与者、消息引用、私密边界及双方讨论都比较明确。

### 桌面可先完成

1. `SceneStore`：统一本地 scene/view/revision/selection，给现有页面增加场景适配器。
2. 围炉资源引用及“和 Being 看这条”交互，捕获不可变环境封套。
3. 本地场景观察面板：直接展示将共享的事实、范围和未知项。
4. 回执状态机与传输适配接口；无 Heart 支持时明确停留在本地，不伪报同步成功。
5. 只读语义操作以及统一动作记录。

### 与 Heart 联调

1. 接收带环境的消息并返回关联 message_id/observation_id 的接收确认。
2. 读取围炉场景状态，并能引用选中的具体消息作答。
3. 返回明确的关注资源与动作意图，客户端展示并执行已支持的只读动作。
4. 重连和跨设备校准通过后，再扩展写入动作、其他页面与场景记忆。

### 场景验收

- 人类选中围炉消息 218，问“你怎么看”；Being 获取当时的房间、明确消息引用与观察范围。
- 消息发送后人类跳到书架，Being 的回答仍绑定原围炉，不借用新的页面状态。
- Being 打开相关卷轴，不抢走人类正在编辑的输入；人类可选择跳过去一起看。
- 卷轴在动作前被其他人修改，旧 revision 的写操作返回冲突，不静默覆盖。
- Portal 在线但桌面关闭，Heart 正确看到 UI 离线及旧快照失效。
- Town 配对身份与当前 Being 不同，私信内容不被默认跨身份共享。
- 再次进入同一围炉，桌面提供稳定 scene_id；Heart 若返回记忆摘要，能与来源消息及时间关联。

## 后续需要两侧确认的协议决定

- Heart 是否能在消息接受回执里确认环境版本，并在历史消息中保留其引用？
- 场景状态通过 Portal 复用通道还是独立流传输，分别如何恢复和确认？
- 参与者、设备、Town 身份与 Being 身份的可信绑定由哪一侧签发？
- Being 关注信号、场景进入信号与唤醒策略如何分离？
- 记忆接口以 scene_id 查询还是由 Heart 自行召回；摘要如何保留来源和不确定性？

## 9. 记忆、SOP 与自主学习（补充需求，尚未实现）

### 需求覆盖核对

| 伙伴提出的理念 | 设计记录 | 当前实现 |
| --- | --- | --- |
| 人与 Being 共同看见、掌握、操作界面 | 场景模型、独立位置、统一动作与回执 | 人类侧场景与引用已实现；Being 侧操作未接通 |
| Portal 感知运行状态，并传给 Being | 进程、Relay、能力和 UI 在线状态分层 | 进程与连接观察已实现；结构化状态广播待接通 |
| 每个工作场景都能感知与同步 | 稳定场景身份、版本、选择、范围 | 部分本地状态已实现；远端同步待接通 |
| 所有渠道的消息都自带真实环境 | 发送瞬间绑定封套；未知事实明确缺失 | 桌面 POST 尝试本地捕获；其他渠道与 Heart 待接通 |
| 经历按场景形成记忆，再次进入可以回想 | scene_id、对象引用、时间和召回来源 | 已记录要求；没有接入记忆读取和召回 |
| Being 已有 SOP，并能自己学习技能 | 记忆、过程性知识、能力成长之间的关系 | 已获得经授权的 Being 整理；原生记忆接口仍待核实 |

### 不应混为一谈的内容

- **经历**：在某个场景，与哪些参与者，围绕哪些对象，做过什么，实际结果如何。成功、失败和中断都可以形成经历。
- **记忆**：Heart 对经历的保存、压缩、关联与回想；包括情境、关系、偏好与过程性知识。不是客户端缓存，也不能用聊天历史替代全部记忆。
- **SOP**：可复用的做事方法，应能说明何时适用、前提、步骤、分支、完成标准、失败处理和不适用范围。是否以独立记忆、卷轴或其他结构保存，以 Heart 的实际实现为准。
- **技能**：Being 形成并能应用的能力，可能依赖多个 SOP、知识与工具，也可能不需要 Kit。一次任务完成、一次自述或 Kit 安装成功不能直接标注为“已经学会”。
- **Kit**：可执行工具及依赖的分发与加载单位。客户端可以核实清单、安装和加载；这些事实不等于 Being 理解或掌握了用法。

“经历 → 反思与记忆 → 形成/修订方法 → 在新情境运用 → 反馈”是设计关系示意，不预设 Heart 一定按此流水线实现。Being 保留发现、尝试、反思和修订的主动性；客户端呈现可核实的来源与结果，不为它编造学习过程。

### 页面应怎样表达

1. **当前场景的关联记忆**：显示 Heart 召回了哪些经历、为何与当前情境有关、发生时间、来源对象、读取范围及未知项。打开记忆仍保留当前工作场景。
2. **这个场景可用的做法**：展示 Being 已有的相关 SOP、适用条件和所需能力；将“查看做法”“整理计划”和“开始执行”分开。展示 SOP 本身不触发执行。
3. **技能与成长**：呈现技能名称、依赖的 SOP/Kit、来源经历、最近有证据的运用、失败与修订。界面区分 Being 的陈述、记录中的验证结果和当前工具可用性。
4. **共同复盘**：围绕已发生的动作和结果讨论“哪些可以复用、哪里需要修正”。是否形成或修改记忆由 Heart 的现有机制决定；客户端不能假装已经写入。
5. **跨场景迁移**：技能来源于场景 A、在场景 B 运用时保留两者引用。环境或依赖变化时明确标出条件差异，不能机械复用旧 SOP。

这些内容应在场景内可见，并能打开独立的记忆/技能详情；不要求用户每次回总聊天页询问“你还记得什么”。完整能力页与 Kit 工具库分别组织，避免把安装工具当作技能学习。

### 与 Heart 对齐的最小信息（提议，不代表现有接口）

- 记忆引用：稳定 ID、来源渠道与场景、关联对象、经历时间、更新时间、可见权限、可回看的来源。
- SOP 引用：稳定 ID 与 revision、适用条件、步骤/分支、预期结果、失败处理、依赖能力、形成和修订依据。
- 技能关联：关联的记忆/SOP/工具、适用范围、实际运用与反馈证据；保留 Heart 的原生状态，不凭前端规则自动升级“熟练度”。
- 学习变化：由谁报告、基于哪次经历、形成/修订了什么、何时发生、是否已持久化。接收事件不等于接受其中的所有判断。
- 执行关联：scene_id、action_id、SOP revision、输入引用和真实结果，使复盘能够回到当时的情境。

先实现只读查看和有出处的关联，再接执行和记忆写入。对话查询得到的自述应标注为“Being 提供的整理”，不能冒充完整记忆数据库或结构化写入回执。

### 本次核实边界（2026-09-09）

已确认当前 Being 在线，可通过已有入口读取状态与对话历史。尝试的记忆类地址返回 Loom HTML 页面，未取得结构化记忆；这只能说明当前入口尚未发现可用的记忆读取接口，不能推断 Being 没有记忆或 SOP。伙伴明确说明 Being 已有 SOP 与自主学习能力；具体实例和存储结构需从正式读取入口或经授权的 Being 整理结果进一步核实。

本仓库仅保存通用设计，不将 Being 的完整私人记忆、凭据或无关个人内容提交进代码仓。

### 经授权查询后的补充

伙伴已明确授权向当前 Being 查询已有 SOP/技能记忆。本次查询收到记忆检索活动及一次卷轴 GET，最终返回了 SOP、技能、约束/模式和未决项的整理。Being 声明仅做只读整理；桌面尚没有独立记忆审计接口可复核其全部内部行为。以下用于完善通用设计，不复制私人记忆全文。

- **SOP 不限于逐步执行流程**：本次返回的显式 SOP 卷轴使用 `kind=guide`，内容兼有行动准则、身份约定、验证标准和学习规则。因此不能只把 `procedure` 卷轴视为 SOP，也不能要求每条准则都具备线性步骤。
- **技能状态应保留来源**：Being 返回了 `Mastered` / `Practicing`，并用工具调用与成功次数描述部分技能。客户端应同时显示原生状态、统计口径、时间范围与来源；当前来源是 Being 的整理，尚未证实这些等级来自独立结构化能力表。
- **失败可以成为学习依据**：返回内容包含连接误判、API 参数错误、内容验证和工作目录等实践教训。技能详情需要能回到形成约束的经历与后来修正，而不是只展示“成功次数”。
- **验证分层**：进程存在、Relay 握手、当前连接、端到端工具真实输出、业务结果正确性分别记录。客户端目前的“已连接”仅陈述连接证据；不得据此宣称工具端到端验证通过，也不为验证状态自动执行记忆中的命令。
- **未决项不是 SOP，也不是能力**：待处理问题、待伙伴决定和待验证步骤应单列，避免被展示成已掌握技能或自动执行队列。
- **转教也是学习结果的一种**：返回的学习准则包括消化和向伙伴解释。界面可承载整理、解释、共同复盘与来源回看，不应把成长压缩成工具调用计数。

独立尝试读取该分享卷轴时，匿名请求返回 401；因此当前具体 SOP 内容仍以经授权取得的 Being 整理为证据，不声称已通过匿名公开接口或完整记忆数据库独立复核。

下一步读取接口应能返回记忆/SOP/技能的稳定引用、原生类型、原生状态、来源和验证范围；详情页明确区分“Being 自述”“记忆系统记录”“客户端可核实的执行结果”。这些是接入要求，尚未作为新页面实现。

